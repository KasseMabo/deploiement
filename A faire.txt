Voici l'ensemble des commandes sur lesquelles vous devez travailler et me faire une démonstration de 7min maximum et par groupe de 2 personnes

git init, git status, git add, git commit, git log, git diff, git config, git help, git rm, git mv


Partie 2:

git clone, git remote, git remote add, git fetch, git pull, git push, git branch -r, git remote -v, git ls-remote, git remote remove

git branch, git checkout, git switch, git merge, git log --graph, git show, git reflog, git cherry-pick, git merge --abort, git stash


Partie 3:

git reset, git reset --hard, git reset --soft, git restore, git revert, git clean, git checkout --, git stash pop, git stash drop, git stash clear, git cherry-pick


Partie 4:

git tag, git describe, git tag -a, git push origin --tags, git show <tag>, git tag -d, git checkout <tag>, git rev-parse, git log --oneline, git archive